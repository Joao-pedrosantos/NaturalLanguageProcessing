# Config que casa com runs/transformer_medium/best.pt (CPU, vocab=4000, ctx=256).
# Pareado com xlstm_medium_legacy.yaml.

vocab_size: 4000
embedding_dim: 256
num_blocks: 4
num_heads: 8
context_length: 256
mlp_expansion: 2
dropout: 0.0
